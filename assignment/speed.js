//write a function with the function name being speed and he parameters The data being used//

function speed(kilometer, hour){

    //declare a variable with condition 
    let x =140;
    let i;

    /*Use of if and else statements.This allows the program to return a value once if the evaluation of the statement returns
    truthy*/
    if(x<= 70){
        console.log("OK");
    }
    else if (x > 70 && x <= 130){

        console.log("C");
     function speedDimerit(demerit){
        for(i=1;i<12;i++){
            //for loop used due to increament of demerit point by everytime speed increases by 5km/hr//
          return
            
        };
        }
    }
  else {
        console.log("License suspended");
    } 
}
speed();